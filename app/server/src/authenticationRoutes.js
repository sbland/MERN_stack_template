const express = require('express');

const router = express.Router();

/* ====== AUTHENTICATION ====== */
// router.post('/api/action/user/register', sessionManager.register);
// router.post('/api/action/user/login', sessionManager.login);
// router.post('/api/action/user/logout', sessionManager.logout);
// router.post('/api/action/user/auth', sessionManager.authorizeSession);
// router.post('/api/action/user/setgroup', sessionManager.setUserGroup);

module.exports = router;